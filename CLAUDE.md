# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`jconsulting` — a fresh repository with no commits yet. Update this file once a tech stack and project structure are established.

## Getting Started

No build system, package manager, or framework has been chosen yet. When one is added, document the following commands here:

- **Install dependencies** — e.g. `npm install` / `pip install -r requirements.txt`
- **Run the app** — e.g. `npm start` / `python main.py`
- **Run tests** — e.g. `npm test` / `pytest`
- **Lint / format** — e.g. `npm run lint` / `ruff check .`

## Architecture

To be defined once the project structure exists. Update this section with:

- The top-level directory layout and what each folder owns
- Key data flows or service boundaries
- Any external services or APIs the project depends on
